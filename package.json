{
  "name": "fcm-server",
  "version": "1.0.0",
  "main": "api/send.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'",
    "start": "vercel dev"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
